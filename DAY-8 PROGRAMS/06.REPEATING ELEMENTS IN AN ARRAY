CODES:
arr = list(map(int, input("Enter array elements: ").split()))
seen = set()
repeated = None
for num in arr:
    if num in seen:
        repeated = num
        break
    seen.add(num)
print("Repeated element:",repeated)


OUTPUT:
Enter array elements: 2,1,3,4,2
Repeated element:2
